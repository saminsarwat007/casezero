# CaseZero API — Python 3.11+

# Web
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
sse-starlette>=2.1.3
httpx>=0.27.0

# Data
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
supabase>=2.10.0

# LLM providers — LLMProvider abstraction sits over these
google-genai>=1.0.0
groq>=0.13.0
openai>=1.54.0          # Hunyuan stub speaks the OpenAI-compatible protocol

# Agent tooling
# Official Model Context Protocol SDK, powering the two stdio servers in
# mcp_servers/. Held below 2.0: that release requires starlette>=1.0, which the
# pinned FastAPI cannot use.
mcp>=1.9,<2

# Documents
pdfplumber>=0.11.4      # digital-text PDFs; Gemini vision handles scans/photos
reportlab>=4.2.5        # FMOS Referral Pack
pypdf>=5.0.0            # PDF structure verification in tests and export QA

# Compliance kernel
PyYAML>=6.0.2
deepdiff>=8.0.1         # policy diffs for the Policy Composer

# Security
cryptography>=43.0.0    # Fernet field encryption
pywebpush>=2.0.3        # customer PWA push
py-vapid>=1.9.1

# Channels
python-telegram-bot>=21.6

# Scheduling (SLA watchdog, fraud-ring clustering)
APScheduler>=3.10.4

# Tests
pytest>=8.3.3
pytest-asyncio>=0.24.0
freezegun>=1.5.1        # deterministic working-day / SLA tests
