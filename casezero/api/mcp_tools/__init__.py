"""Tools exposed to agents over the Model Context Protocol.

The implementations live here so they can be unit-tested without a subprocess;
`mcp_servers/` wraps them in two real MCP stdio servers.
"""
